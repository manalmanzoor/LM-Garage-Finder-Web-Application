<IfModule mod_rewrite.c>
	<IfModule mod_negotiation.c>
		Options -MultiViews -Indexes
	</IfModule>

	RewriteEngine On
	RewriteBase /

	# Forward any request under /garages to the front controller (keep query string)
	RewriteRule ^.*$ /index.php [L,QSA]
</IfModule>
